@model AdWebApp.Models.ClientProfileModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete profile</h2>
<div class="container">
    <hr>

    <div class="form-horizontal">

        <div class="form-group row">
            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "col-md-1" })
            <div class="col-md-4">
                @Html.DisplayFor(model => model.Name, new { htmlAttributes = new { } })
            </div>
        </div>

        <div class="form-group row">
            @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "col-md-1" })
            <div class="col-md-4">
                @Html.DisplayFor(model => model.Email, new { @class = "control-label" })
            </div>
        </div>

        <div class="form-group row">
            <div class="col-md-offset-2 col-md-8">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    @Html.ActionLink("Back to List", "Index", "Manage", new { @class = "btn btn-primary btn-sm" })
                    <input type="submit" value="Delete" class="btn btn-primary btn-sm"/>
                }
            </div>
        </div>
    </div>
</div>
